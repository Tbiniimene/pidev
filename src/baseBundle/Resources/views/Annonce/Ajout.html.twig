{% extends '@base/admin/base.html.twig' %}
{% block title %}Admin - Article{% endblock %}
{% block body %}
    <script src="{{ asset('admin/vendor/jquery/jquery.min.js') }}"></script>
    <script>
        $(document).ready(function()
        {
            // $("#eventImg").on('change',function()
            // {
            //     console.log($(this).val());
            // }) ;
        });
    </script>
    <!-- Page Content -->
    <div class="card mb-3">
        <div class="card-header">
            <i class="fas fa-table"></i>
         Article List
        </div>
        <div class="card-body">
            <div class="table-responsive table-hover">
                <table class="table table-bordered" id="dataTable" width="100%" cellspacing="0">
                    {{ form_start(form) }}

                    <tr>
                        <td>id : </td>
                        <td>{{ form_widget(form.id_Annonce) }}</td>
                    </tr>

                    <td>nom : </td>
                    <td>{{ form_widget(form.nom) }}</td>

                    <tr>
                        <td> date: </td>
                        <td>{{ form_widget(form.date) }}</td>
                    </tr>
                    <tr>

                        <td>description : </td>
                        <td>{{ form_widget(form.description) }}</td>
                    </tr>
                    <tr>
                        <td>titre : </td>
                        <td>{{ form_widget(form.titre) }}</td>
                    </tr>
                    <tr>
                        <td>image : </td>
                        <td>{{ form_widget(form.file) }}</td>
                    </tr>

                </table>
                <tr> <center>  <button class="btn btn-primary" type="submit">Add Article</button></center>
                    {{ form_end(form) }}
                </tr>


            </div>
        </div>
    </div>
{% endblock %}