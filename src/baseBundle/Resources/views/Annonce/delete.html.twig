{% extends '@base/admin/base.html.twig' %}
{% block title %}Admin - articles{% endblock %}
{% block body %}
    <!-- Page Content -->
    <div class="card mb-3">
        <div class="card-header">
            <i class="fas fa-table"></i>
            article List
        </div>
        <div class="card-body">
            <div class="table-responsive table-hover">
                <t {{ form_start(form) }}
                <tr>
                    <td>id : </td>
                    <td>{{ form_widget(form.id) }}</td>
                </tr>


                </table>
                <br>
                <center><button class="btn btn-primary" type="submit">delet</button></center>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}