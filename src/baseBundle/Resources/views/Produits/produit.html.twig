{% extends '@base/admin/base.html.twig' %}
{% block title %}Admin - Blank{% endblock %}
{% block body %}

    <html>
    <head></head>
    <body>
    <center>
        <h1>Liste des produits</h1>
        <table border="1">
            <tr>

                <th>produit</th>
                <th>image</th>
                <th>owner</th>
                <th>qte</th>
                <th>prix</th>
                <th>delete</th>
                <th>edit</th>
            </tr>

            {% for e in Produits %}
            <tr>

                <td> {{ e.nom}} </td>
                <td> <img src="{{ asset(e.image) }}" style="width: 50px;height:50px" /></td>
                <td> {{ e.owner }} </td>
                <td> {{ e.qte }} </td>
                <td> {{ e.prix }} </td>
                <td> <a href="{{ path('delete', {'idProduit':e.idProduit}) }}">delete</a></td>
                <td> <a href="{{ path('edit', {'idProduit':e.idProduit}) }}">edit</a></td>
                <td></td>
                {% endfor %}

        </table> </table>  <a href="{{ path('ajout') }}"/> ajout

    </center></body></html>

{% endblock %}